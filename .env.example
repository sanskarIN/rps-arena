# RPS Arena v1 requires no runtime environment variables.
#
# Keep this file as an explicit reminder that API keys, signing passwords,
# production tokens, private endpoints, and generated secrets do not belong
# in the public repository.
#
# Future optional integrations must add placeholder NAMES only, never values.

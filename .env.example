# RPS Arena v1 requires no runtime environment variables.
#
# This public repository must never contain real API keys, signing passwords,
# private endpoints, generated secrets, tokens, or credentials.
#
# If a future optional integration needs configuration, add placeholder names
# here without real values and document the least-privilege setup separately.
